import { DurationTransformPipe } from './duration-transform.pipe';

describe('DurationTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new DurationTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
