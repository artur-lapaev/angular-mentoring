import { BorderDateDirective } from './border-date.directive';

describe('BorderDateDirective', () => {
  it('should create an instance', () => {
    const directive = new BorderDateDirective();
    expect(directive).toBeTruthy();
  });
});
